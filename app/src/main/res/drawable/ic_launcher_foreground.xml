<?xml version="1.0" encoding="utf-8"?>
<!-- All artwork lives in ic_launcher_background.png (the actual icon image supplied), so the
     foreground layer stays empty rather than drawing anything on top of it. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108" />
